import SwiftUI

struct GeneticView: View {
    var body: some View {
        VStack {
            Text("Генетический алгоритм — Обед")
                .font(.title2)
                .bold()
            Text("В разработке")
                .foregroundColor(.gray)
        }
    }
}
