import SwiftUI

struct AntView: View {
    var body: some View {
        VStack {
            Text("Муравьиный алгоритм — Прогулка")
                .font(.title2)
                .bold()
            Text("В разработке")
                .foregroundColor(.gray)
        }
    }
}
