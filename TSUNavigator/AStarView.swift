import SwiftUI

struct AStarView: View {
    var body: some View {
        VStack {
            Text("A* — Построение маршрута")
                .font(.title2)
                .bold()
            Text("В разработке")
                .foregroundColor(.gray)
        }
    }
}

#Preview {
    AStarView()
}
