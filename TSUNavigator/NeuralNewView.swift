import SwiftUI

struct NeuralNetView: View {
    var body: some View {
        VStack {
            Text("Нейронная сеть — Оценка")
                .font(.title2)
                .bold()
            Text("В разработке")
                .foregroundColor(.gray)
        }
    }
}
