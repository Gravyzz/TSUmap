import SwiftUI

struct ClusteringView: View {
    var body: some View {
        VStack {
            Text("Кластеризация — Зоны кафе")
                .font(.title2)
                .bold()
            Text("В разработке")
                .foregroundColor(.gray)
        }
    }
}
