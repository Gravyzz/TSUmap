import SwiftUI

struct DecisionTreeView: View {
    var body: some View {
        VStack {
            Text("Дерево решений — Советник")
                .font(.title2)
                .bold()
            Text("В разработке")
                .foregroundColor(.gray)
        }
    }
}
